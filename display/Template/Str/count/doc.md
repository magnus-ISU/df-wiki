<!-- Mirrored from https://dwarffortresswiki.org/index.php/Template:Str/count/doc (revision 190538).
     Generated by crawl.py - edit the wiki, not this file. -->

# Template:Str/count/doc

*[Source](https://dwarffortresswiki.org/index.php/Template:Str/count/doc) &middot; revision 190538 &middot; retrieved 2026-09-13 &middot; [wikitext](../../../../mirror/wikitext/Template/Str/count/doc.wiki)*

---
Essentially {{[str/len](../len.md)}}, but counts the number of times {{{1}}} appears in {{{2}}}.

Note that {{{1}}} can be any [regular expression](http://en.wikipedia.org/wiki/regular_expression).

### Examples

```
{{str/count|a|abca}}
```

→ 2

```
{{str/count|\d|abc 123}}
```

→ 3

```
{{str/count|\D|abc 123}}
```

→ 4

```
{{str/count|\w+|this is an example}}
```

→ 4 (`\w+` matches one or more "word" characters, but not spaces)

```
  {{str/count|\[\[[^]]+\]\]|This is text with a [[link]] and [[another link]].}}
```

→ 2

Breakdown of regular expression:

| Section | Meaning | Explanation |
| --- | --- | --- |
| `\[\[` | `[[` | Two *literal* `[` characters (note that `[` and `]` have a special meaning in regular expressions). |
| `[^]]` | Any character that is not `]` | A character class -- matches all characters listed between the brackets. The `^` is the negation operator, causing all characters *not* listed to be matched instead. In this case, the first `]` is the only character matched, since the second is the closing bracket. The first bracket is not treated as the closing bracket, since an empty character class is invalid. |
| `+` | Allows one or more matches of the previous expression | (in this case, it allows the non-`]` characters to be repeated) |
| `\]\]` | `]]` | Matches two literal `]` characters. |

---

**Categories:** [Template Documentation](../../../Category/Template_Documentation.md)

---

*Mirror of the [Dwarf Fortress Wiki](https://dwarffortresswiki.org); content is under the wiki's [copyright terms](https://dwarffortresswiki.org/index.php/Dwarf_Fortress_Wiki:Copyrights).*
